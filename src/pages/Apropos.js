import React from 'react';
import Menu from '../Menu';

const Apropos = () => {
    return (
        <div>
            <Menu/>
               <h1>A propos</h1>
        </div>
    );
};

export default Apropos;